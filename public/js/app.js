const app = {
  init: function () {
    // user.init();
    quest.init();
    //   form.init();
    //   grid.generate(8);
    //   palette.init();
  },
}

document.addEventListener('DOMContentLoaded', app.init);